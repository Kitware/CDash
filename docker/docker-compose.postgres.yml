services:
  cdash:
    environment:
      DB_CONNECTION: pgsql
      DB_LOGIN: postgres
      DB_PORT: 5432

  database:
    image: postgres
    container_name: cdash_postgres
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
